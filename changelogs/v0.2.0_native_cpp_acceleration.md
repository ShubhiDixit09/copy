# Release v0.2.0 - Native C++ Acceleration & PCI Engine
**Date:** 2026-09-05  
**Commit:** `d82a363`  

---

### Added
- **Native C++ Engine** (`src/native/pci_engine.h`, `src/native/pci_engine.cpp`):
  - High-speed interval merging algorithm in modern C++17.
  - Possession Continuity Index (PCI) computation ($PCI = \frac{L_{\text{continuous}}^{\max}}{L_{\text{total}}}$).
  - Combinatorial $O(N)$ corridor unlock simulation ranking bottleneck parcels by frontage gain.
- **Dual-Engine Architecture**:
  - `src/services/pci/pci_service.py` with dynamic `ctypes` loading and pure Python fallback.
- **Automated Verification**:
  - Added unit test `tests/unit/test_pci_engine.py` validating identical results between native C++ and fallback Python algorithms.
